services:
 client-ui:
  build:
   context: .
   dockerfile: Dockerfile
  ports:
    - "8001:8001"
     #volumes:
     #- .:/app
     #- /usr/src/app/node_modules
  environment:
   - NODE_ENV=production
   - REACT_APP_SERVER_URL=https://keelmarket.co.uk
  command: nginx -g "daemon off;"


